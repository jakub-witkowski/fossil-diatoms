{# {% form_theme taxonForm 'tailwind_2_layout.html.twig' %} #}

{% extends 'atlas_base.html.twig' %}

{% block body %}

<div class="text-xl text-gray-700 font-semibold text-center mx-auto py-6">
    <span><i>Search for taxon</i></span>
</div>

<div class="w-2/5 mx-auto">
    {{ form_start(taxonForm) }}
        {{ form_widget(taxonForm) }}
        <button type="submit" class="btn btn-primary">Search</button>
    {{ form_end(taxonForm) }}
</div>

{% endblock %}
