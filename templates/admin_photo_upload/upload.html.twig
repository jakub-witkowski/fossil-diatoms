{% extends 'base.html.twig' %}

{% block body %}

<h1>Upload a photo</h1>

{#    <form method="POST" action="{{ path('upload_test') }}" enctype="multipart/form-data">#}
{#        <input type="file" name="image">#}
{#        <button type="submit">Upload!</button>#}
{#    </form>#}

    <div class="mx-2 py-2 lg:w-2/5 lg:mx-auto lg:py-6">
        {{ form_start(photoUploadForm) }}
            {{ form_row(photoUploadForm.imageFile, {
                attr: {
                    placeholder: 'Select an image'
                }
            }) }}
        {{ form_widget(photoUploadForm) }}

{#        <div class="">#}
{#            {% if photoUploadForm.vars.data.Filename %}#}
{#                <a href="{{ uploaded_asset(photoUploadForm.vars.data.Filename) }}" target="_blank">#}
{#                    <img src="{{ photoUploadForm.vars.data.Filename }}" height="100">#}
{#                </a>#}
{#            {% endif %}#}
{#        </div>#}

        <div class="flex w-full place-content-end">
            <button type="submit" class="border-2 border-gray-700 bg-gray-700 font-black text-xl text-white hover:text-gray-300 py-2 px-4 rounded-xl">Upload</button>
        </div>
        {{ form_end(photoUploadForm) }}
    </div>

{% endblock %}