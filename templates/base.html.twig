<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="w-2/3 my-0 mx-auto">
        <header class="relative border-b border-black pb-0">
            <h1 class="text-4xl font-bold">fossil-diatoms.com</h1>
            
            <div class="absolute top-10 right-0">
                <a href="https://www.instagram.com/fossil.diatoms/" target="_blank"><img class="w-8" src= {{ asset('images/ig_icon.png') }} alt="Instagram icon"></a>
                <a href="https://www.facebook.com/fossil.diatoms" target="_blank"><img class="w-8" src= {{ asset('images/fb_icon.png') }} alt="Facebook icon"></a>
            </div>

            <nav class="text-center border-t border-black">
                <div class="dropdown">
                    <button class="dropbtn"><a href="?page=home.php">Strona główna</a></button>
                </div>
    
                <div class="dropdown">
                    <button class="dropbtn"><a href="atlas/index.php">Fossil Diatom Atlas</a></button>
                </div>
        
                <div class="dropdown">
                    <button class="dropbtn">Materiały dla studentów</button>
            
                    <div class="dropdown-content">
                        <a href="?page=zasoby-hz">Historia Ziemi (wykład ogólnouczelniany)</a>
                        <a href="?page=zasoby-grp">Geologia regionalna Polski (II rok, geologia 1°)</a>
                        <a href="?page=zasoby-gh">Geologia historyczna (II rok, geologia 1°)</a>
                    </div>
                </div>

                <div class="dropdown">
                    <button class="dropbtn">O mnie</button>

                    <div class="dropdown-content">
                        <a href="?page=o-mnie">Autor</a>
                        <a href="?page=publications">Publikacje</a>
                    </div>
                </div>

                <div class="dropdown">
                    <button class="dropbtn"><a href="?page=kontakt">Kontakt</a></button>
                </div>
            </nav>
        </header>

        <main class="mx-2.5 mt-2.5">
            {% block body %}{% endblock %}
        </main>

        <footer class="mt-2.5 border-t border-black">
            <div class="mx-2.5 my-2.5 text-right text-base">
                &copy; Jakub Witkowski, {{ year }}</p>
            </div>
        </footer>
    </body>
</html>
